const logic = require('../../logic')

describe('check Male', ()=>{

    it('gender is male', ()=>{
        expect(logic.isMale('Male'))
            .toEqual(true)
    })
})

describe('check Male', ()=>{

    it('gender is male', ()=>{
        expect(logic.isMale('Male'))
            .toEqual(true)
    })
})

describe('check Male', ()=>{

    it('gender is male', ()=>{
        expect(logic.isMale('Male'))
            .toEqual(true)
    })
    
})

describe('check Male', ()=>{

    it('gender is male', ()=>{
        expect(logic.isMale('Male'))
            .toEqual(true)
    })
})

describe('check Male', ()=>{

    it('gender is male', ()=>{
        expect(logic.isMale('Male'))
            .toEqual(true)
    }) 
})

describe('check Male', ()=>{

    it('gender is male', ()=>{
        expect(logic.isMale('Male'))
            .toEqual(true)
    })
})